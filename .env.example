# Optional: location of the raw OSV5M dataset.
# It must contain test.csv, train.csv, and images/.
OSV5M_DATA_DIR=./data/osv5m

# Optional: use a downloaded local model instead of Hugging Face.
# OSV5M_BASELINE=./codes/baseline-huggingface/baseline
